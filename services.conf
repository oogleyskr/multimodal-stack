# services.conf — Configurable list of enabled multimodal services.
#
# This file is sourced by start-all.sh, stop-all.sh, and status.sh.
# Edit ENABLED_SERVICES to control which services are started by default.
#
# Override at runtime with the MULTIMODAL_SERVICES env var:
#   MULTIMODAL_SERVICES="stt tts" bash /home/mferr/multimodal/scripts/start-all.sh
#
# Available services:
#   stt       - Speech-to-Text (faster-whisper, port 8101, ~3GB VRAM)
#   vision    - Vision/VLM (Qwen2.5-VL-7B-AWQ, port 8102, ~5GB VRAM)
#   tts       - Text-to-Speech (Kokoro-82M, port 8103, ~0.5GB VRAM)
#   imagegen  - Image Generation (SDXL-Turbo, port 8104, ~5GB VRAM)
#   embeddings - Text Embeddings (nomic-embed, port 8105, ~0.5GB VRAM)
#   docutils  - Document Parsing (CPU-only, port 8106)
#   findata   - Financial Data (port 8107)

# Default enabled services (the 4 core GPU services)
ENABLED_SERVICES=(stt vision tts imagegen)
